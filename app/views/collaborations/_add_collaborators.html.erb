<%= form_tag c_m_collaborations_path, method: :post do %> 
  <ul class="user-list">
    <% users.each do |user| %>
        <li><%= check_box_tag "collaborators[]", user.id %><%= user.email %></li>
    <% end %>  
  </ul>  
  <%= submit_tag "Add collaborator" %>
<% end %>